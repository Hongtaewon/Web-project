<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="project.backend.dao.userDAO"><!--namespace를 통해 UserDAO(interface)와 연결합니다. -->
  		
  <select id="findAll" resultType="userDTO">
  	SELECT 
  		id,
  		name,
  		phone_number,
  		email 
  	FROM user
  </select>
  
  <select id="find" parameterType="String" resultType="userDTO">
  	SELECT 
  		id,
  		name,
  		phone_number,
  		email
  	FROM user 
  	WHERE id = #{id}
  </select>
  
  <insert id="save" parameterType="userDTO">
        INSERT INTO user (
            id,
  			name,
  			phone_number,
  			email
        ) VALUES (
            #{id},
            #{name},
            #{phone_number},
            #{email}
        )
    </insert>
    
    <update id="update" parameterType="userDTO">
        UPDATE user
        SET 
            id = #{id},
            name = #{name},
            phone_number = #{phone_number},
            email = #{email}
        WHERE id = #{id}
    </update>
    
</mapper>